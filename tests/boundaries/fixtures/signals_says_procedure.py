# NEGATIVE fixture (r6): word boundaries. `/procedure` is not `/proc`, and a
# rule that says otherwise is a rule someone will add an exemption to.
DOC_PATH = "/procedure/onboarding.md"
NOTE = "follow the procedure"
