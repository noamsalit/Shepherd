"""Stop the selected session.

NEGATIVE fixture (r6): `Stop`, `Setup`, `Notification`, `FileChanged` and
`MessageDisplay` are five of the 33 hook event names AND ordinary English. This
module is prose about stopping a session, not the engine's vocabulary.
"""


def help_line() -> str:
    """Shows a Notification when Setup completes."""
    return "see the docstring"
