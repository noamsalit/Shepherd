# T5.1 — D66, in the same task that deletes the assertion

**Base SHA:** `e4a0a75256c08fa8617d64aa7b6406ae8bf5cef6`
**Branch:** `worktree-agent-ab17891c0d50ee88f`
**Date:** 2026-09-22

## What this task did

1. Deleted `tests/web/test_rail.py` (seven frozen node ids).
2. Wrote **D66** into §3 of `docs/specs/orchestrator-platform.md` — in the same
   commit, which is the point of the task and not an implementation detail. §0
   forbids reversing a numbered decision quietly, and
   `test_rail_is_on_every_page` is §12's *"on every page"* rule in executable
   form.
3. Moved §3's own headline count with the row: line 34 read *"**65 decisions**
   (D1–D65, plus D38.1, an amendment)"* and now reads **66** / D1–D66. A
   section whose own count is wrong has stopped being checkable.
4. Rewrote §12's rail section to point at D66. The mock stays, labelled as a
   record rather than a placement spec; the **content** rules (the actual ask
   never a category, empty ≠ not-read-yet, three row kinds) are called out as
   *not* reversed and re-anchored at `tests/qa/test_s4_needs_you_rail.py`.
5. Added seven `RETIRED_NODE_IDS` entries, decision string `"§3 D66"`, each
   reason over 80 characters and each naming where the surviving half of that
   test's property is now asserted.
6. Retargeted `tests/qa/test_s4_needs_you_rail.py`'s prose at the projection.
   Its assertions were **already** at `project_needs_you` / `fleet_summary`, so
   none of them moved — what changed is three passages that pointed a reader at
   the deleted module as the place the rail's rendering is checked.

## What did not change, deliberately

`tools_m1.py::project_needs_you` and `fleet_summary`'s `needs_you` list are
untouched — approval rows included. Only the renderer goes, and `rail.js` itself
is **T5.3's**, not this task's. `index.html`, `app.css`, `app.js`,
`web/routes.py` and `tests/boundaries/consumer_manifest.json` were not touched:
another builder holds `routes.py`, the shell markup is T5.3's, the manifest is
T5.4's.

## The gate, seen to fail

| Step | Command | Exit | Result |
| --- | --- | --- | --- |
| RED — module deleted, no entries | `pytest tests/boundaries/test_collected_node_ids.py -q` | 1 | 2 failed: *"node ids frozen at step 0b are gone with no recorded decision: ['tests/web/test_rail.py::…']"* |
| GREEN — seven entries written | same | 0 | 7 passed |
| Mutation control — one entry removed from the final tree | same | 1 | the removed id is named as unrecorded |
| Full suite | `pytest -o "addopts=-m 'not live' -q"` | 0 | 2016 passed, 2 skipped, 75 deselected |

## What this does NOT prove

- Nothing about the new shell. `rail.js`, `index.html` and the manifest still
  describe a rail that is on the page; D66 records the decision, T5.3 and T5.4
  carry it out. Between this commit and T5.4 the spec and the shipped markup
  disagree, and that is the plan's declared order.
- Nothing rendered. There is no browser on this host; every surviving assertion
  is at the projection seam.
- `docs/design/ui-decisions.md:272` still says *"it needs a decision row of its
  own … Do not delete the assertion quietly."* That is now satisfied by D66, but
  the file is outside this task's Files list and was left alone. A later task
  should strike the instruction or mark it done.
