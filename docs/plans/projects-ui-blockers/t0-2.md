# T0.2 — the blockers ledger, one file per task

Status: **done**. No blockers.

## What was built

`docs/plans/projects-ui-blockers/README.md` and the directory it names. The README states the
six rules the plan's Exit Criteria require: one file per task under
`docs/plans/projects-ui-blockers/<task-id>.md`; nobody opens another task's file; the entry
shape `T<id> · symptom · what was tried · what it blocks · owner`; ids are never reused (with
the retired Phase 2 as the worked example); an entry is never deleted, only retired in place;
**never a shared ledger**.

## What was deliberately not done

**No entry was written into the README** — that is this task's declared Out-of-Scope Drift.
T0.1's findings are in `t0-1.md`, this task's in this file, which is the rule demonstrating
itself on its first use.

## Check

`test -d docs/plans/projects-ui-blockers` → exit 0. Validation level: manual (file exists),
as declared.
