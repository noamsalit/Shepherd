# M1 blockers

One line per blocker: `T<id> · symptom · what was tried · what it blocks · owner`.
Append only.

T3/T4 · `MacHost`'s supervision, process liveness and login persistence refuse off-platform (`UnverifiedHostCapability`) instead of answering, and `hook_dispatch()` reports `available=False` · every value in `host/mac.py` is written and annotated `# UNVERIFIED (no capture) — D55`; the platform-independent half (path composition, 103-byte budget arithmetic, record shapes, the dispatch command's shape) runs in the contract suite on Linux, and the real-host cases are skipped, never faked · blocks M6 packaging on macOS and any macOS install of the hook command · **evidence gap** — closing it needs one macOS run capturing: (1) `sun_path` overflow boundary (bind at 103 and 104 bytes), (2) the netcat flag set (`nc -U` with and without `-q0`, timed as in `docs/probes/2026-09-16-hookd-latency.md` Result 1b), (3) `launchctl print gui/<uid>` output shape, (4) `ps -o lstart= -p <pid>` as a start token across a pid reuse, (5) `~/Library/Application Support` / `~/Library/Preferences` / `TMPDIR` as the real directories. Until then `verified()` is False and the values are marked `unverified`, not BLOCKED.

BLOCKER-T6-1 (T5/T6) · `SessionSnapshot` carries four fields with no `§7` column — `observed_at` and the three fold identity sets (`live_subagent_ids`, `created_task_ids`, `completed_task_ids`) — so `Store.snapshot()` returns them at their empty value and `Store.apply_fold_delta()` silently has nowhere to put `FoldDelta.observed_at` · the `session` columns were written exactly to T5's Allowed Scope (§7 plus Decision pressure 7's `pid`/`proc_start`), and the gap was left visible behind `tests/store/test_verbs.py::test_snapshot_roundtrips_the_fold_identity_fields` (`xfail(strict=True)`, this id) rather than closed by inventing a column · blocks RD8's per-field recency rule (T11) and any subagent/task de-duplication that must survive a `controld` restart · **decision gap**, in the plan's four-part form:
  - **decision** — §7's `session` table and T5's Allowed Scope, which forbid columns the spec does not list.
  - **pressure** — `core/fold_types.py` (T1, already shipped and green) defines `SessionSnapshot` with `observed_at` and three `frozenset` identity fields, and `FoldDelta` with `observed_at`. T6's `snapshot()` is specified to return a `SessionSnapshot`, so either those four fields are persisted or the fold's prior input is partly a fiction after every process restart. Nothing in T5's or T6's Required Checks names them, so no task owns the problem.
  - **options** — (a) four columns on `session`: `observed_at TEXT`, and three JSON columns for the id sets — honest, queryable, and a widening of §7 in the same shape as Decision pressure 7; (b) one JSON `fold_state` column holding all four — one migration line, but a schema hiding inside a value column, and every reader needs bespoke code; (c) keep them process-local in `controld`'s memory — no schema change, and the sets are correct while the daemon lives, but a restart silently resets subagent and task identity, which is the same class of bug Decision pressure 7 rejected option (b) for.
  - **recommendation (~~NOT applied~~ — APPLIED, see below)** — (a), as a flagged §7 widening in the Plan-vs-spec gaps table, decided together with T11's fold rules so the id sets' retention is specified rather than implied.
  - **APPLIED 2026-09-16 (remediation) — option (a), and this entry is no longer open.** Four columns on `session`: `observed_at TEXT`, and `live_subagent_ids` / `created_task_ids` / `completed_task_ids` as JSON arrays read back whole. Recorded as a flagged §7 widening in the plan's Plan-vs-spec gaps table, **row M16**, exactly as the recommendation asks. The columns were added to `001_m1_foundation.sql` in place rather than as a `002`, following the precedent this milestone already set for `repo.git_common_dir` (entry T5/T6/T7): migration 001 has never been applied anywhere but a dev database, `EXPECTED_SCHEMA_VERSION` stays 1, and the forward-only rule starts binding at the first shipped schema. `FoldDelta` carries the three sets as **whole values** (the delta says what the set now is, never what to add), so `None` still means *untouched* and an empty set is a real clear; `fold()` puts them on the delta so they cross the same one writer as the counts computed from them (D37). `db.py` was split — `store/rows.py` now owns row → dataclass — because the new columns pushed it past ADR-1's 600-line cap. Proven by `tests/store/test_verbs.py::test_snapshot_roundtrips_the_fold_identity_fields` (the strict `xfail` is removed, not relaxed) and by `tests/signals/test_fold_rules.py::test_subagent_pairing_survives_a_controld_restart`, which closes and reopens a real store on the same file so the only path from the start to the stop is the database.

T5/T6/T7 · `repo.git_common_dir` is a §7 widening that T5's Allowed Scope does not list but T6's and T7's own Produces blocks require (`upsert_repo(..., git_common_dir)`, `find_repo_by_common_dir`, D48) · the column was added to `001_m1_foundation.sql` with a comment citing D48, un-indexed (§7's index list is closed; the table is tens of rows) · blocks nothing — recorded so the widening is not discovered later as an undocumented drift · **decision gap, applied under the plan's own instruction**: D48 replaced longest-prefix matching, and the binding key has to live somewhere; a reviewer who rejects the column must also reject `find_repo_by_common_dir`.

T11 (from T2 r6) · the widened `Signal.fields` rule **fails closed on whole-mapping reads**, so `dict(signal.fields)`, `{**signal.fields}`, `.keys()`, iteration, a computed key, and passing `.fields` to any callee are now build failures inside `signals/` · implemented exactly as plan r6 requires ("a computed key cannot be checked… if a dynamic key is ever genuinely needed it arrives as a named plan change, not as a silent hole"), and verified not to fire on the shipped `signals/` modules (`discovery.py`, `binding.py`) · blocks nothing today; it constrains T11's rule table and T13's subagent rollup, which must read named neutral keys one at a time · owner **T11** — if the fold genuinely needs the whole mapping, that is a plan change, not an exemption.

T12 (from T2 r6, finding C1) · `new_ulid(now=...)` changed semantics: the injected clock is now **authoritative**, so a *backwards* injected clock yields a backwards-sorting id, where the r5 docstring promised ordering could never regress · the two lanes were separated (wall clock keeps its anti-regression counter; the injected lane restarts a deterministic counter per injected millisecond) because the shared global silently discarded any injected `now` below the highest wall clock a previous caller had seen — measured: fresh process gave a Nov-2023 prefix, after one wall-clock call the same argument gave a 2026 prefix · blocks nothing; it is what makes T12's golden-fixture lane reproducible, and all three shipped call sites (`store/db.py:326,:353,:401` — a fourth occurrence at :32 is the import, not a call) use the no-argument wall-clock lane · owner **T12** — assert decoded prefixes in the golden fixtures rather than relying on call order.

BLOCKER-T6-2 (found by T8/T9) · `tests/store/test_verbs.py::test_reads_use_a_thread_local_connection` fails intermittently with `assert 2 == 5` (or 3, or 4) — it keys `seen` by `threading.get_ident()` and asserts five distinct ids, but the four worker threads are so short-lived that they run one after another and **CPython recycles the ident**, so two threads that never coexisted share one id · reproduced with **no Shepherd code involved at all**: `python -c` with four sequentially started no-op threads printed `len(set(idents))` = 2, 1, 1 on three consecutive runs; and with `tests/conftest.py` moved aside (i.e. the pre-T8 tree) `pytest tests/contracts tests/store/test_verbs.py::test_reads_use_a_thread_local_connection` already fails, while `pytest tests/store` alone passes — so this is a **pre-existing latent defect in the test, exposed by file ordering**, not a regression from T8/T9 and not a defect in `Store` (the thread-local connection ids in the failure output are all distinct; only the *keys* collide) · blocks nothing functionally, but it makes `pytest tests` red at any ordering that serialises those threads, which will keep re-appearing as tasks are added · owner **T6 — test defect**: the fix is to key the observation by something the test controls (a per-thread index, or a barrier that forces the four readers to coexist) rather than by a recyclable ident. Not applied here: `tests/store/` is outside T8/T9's allowed surface.

T9 · `inspect_hooks(settings_path)` as specified cannot answer its own `command_matches_current_host` field — the file says what command is installed, and nothing in the signature says what the current host's command is · the parameter list was widened **additively** to `inspect_hooks(settings_path: Path, entry: HookEntry | None = None)`, so every call the plan spells still type-checks and still works; with no `entry` the field is **False**, documented as "unknown is not 'matches'" (principle 5) rather than defaulting to True and reporting a match it cannot see · blocks nothing — recorded so the widening is not later discovered as undocumented drift · **decision gap, applied**: the alternative was for `inspect_hooks` to call `detect_host()` itself and invent a socket name, which would put composition-root knowledge (which socket, under which runtime dir) inside an inspector and make it untestable without a host.

T10 · `SO_PEERCRED` has no home on the six-member `HostPlatform` seam, so the ingest listener cannot read peer credentials without either naming a Linux-only socket option inline (D55 forbids it outside `host/`) or growing the seam to seven members (D55 says exactly six, and Decision pressure 2 already spent the sixth on `hook_dispatch`) · `serve_ingest` takes an injected `peer_credentials: Callable[[socket.socket], int | None] | None = None`; with nothing injected `Frame.peer_pid` is `None`, an honest unknown (principle 5) rather than a guess, and T10's required checks do not depend on it. E21 says credentials are captured at `connect()` and that walking `/proc` from them races the hook's exit, and the §"Hook runtime contract" capture says `CLAUDE_PID` in the frame equals the owning pid — so the frame, not the socket, is the authoritative source and nothing in M1 is blocked · blocks: nothing in M1; it blocks any future caller that wants a socket-derived pid (a hookless or spoof-resistant path) · owner: decision gap
  - decision: where does `SO_PEERCRED` live?
  - pressure: D55 fixes `HostPlatform` at six members; the option is Linux-only; `tests/boundaries/test_platform_branching.py` keeps platform facts inside `host/`.
  - options: (a) leave it injected and unused in M1, `peer_pid=None` — what is built; (b) add a seventh seam member `peer_credentials(sock)` and amend D55; (c) put a `peercred()` module function in `host/linux.py` and `host/mac.py` (macOS has `LOCAL_PEERCRED`, uncaptured) that the composition root injects — no seam change, and the platform fact stays in the one package that may hold it.
  - recommendation (**not applied**): (c), and only when a caller actually needs the pid. It keeps D55's six members intact, keeps the Linux-only constant inside `host/`, and the injection point already exists.

T7b-1 · `registry_delta` is specified to return **`FoldResult`**, which T11 owns in `signals/fold.py` — a file being written concurrently and absent from the tree when T7b was built · a structurally identical `FoldResult` (`delta`, `events`, `anomalies`) is defined in `signals/discovery_loop.py` with a docstring pointing here; importing the absent module would have made every T7b test an ImportError, and defining it in `core/` was outside T7b's allowed surface · blocks: nothing today — it blocks T18's composition root, which will have **two structurally identical `FoldResult` types** and one `on_result: Callable[[FoldResult], None]` that can only name one of them · owner: **T11 + T18 — decision gap**
  - decision: where does `FoldResult` live?
  - pressure: two lanes (hook and registry) must return the same type for D37's "one writer, one rule set" to mean anything, and both lanes were built in parallel by different builders.
  - options: (a) move `FoldResult` to `core/fold_types.py` beside `FoldDelta`/`SessionSnapshot`, and have both `signals/fold.py` and `signals/discovery_loop.py` import it — the same treatment F9 already applied to `FoldDelta`, and the reason that treatment exists; (b) `signals/discovery_loop.py` imports it from `signals/fold.py` once T11 lands — one definition, but it makes the hookless lane depend on the hook lane's module for a type it does not otherwise need; (c) leave both — a latent type split that `mypy --strict` will only catch at the call site that mixes them.
  - recommendation (~~**not applied**~~ — **APPLIED**, see below): (a). It needs one edit in `core/` and two import lines, and it is the decision F9 already made for the sibling type.
  - **APPLIED 2026-09-16 — option (a), the treatment F9 already gave the sibling type.** `FoldResult` now lives in `core/fold_types.py` beside `FoldDelta`; both `signals/fold.py` and `signals/discovery_loop.py` import it and re-export it, so every existing import path still resolves and T18 has one type to name. `IdentitySets`/`EMPTY_SETS` moved with it (the hook lane's `FoldResult` carries a fourth field and `core/` may not import `signals/`), and `FoldResult.sets` defaults to empty so the registry lane, which pairs nothing, constructs it unchanged. Policed by `tests/test_core_types.py::test_both_fold_lanes_return_the_same_type_and_the_same_clear`, which asserts the two lanes' `FoldResult` are the *same object*, not merely the same shape.

T7b-2 · `claude agents --json` is a **projection** that omits `entrypoint` and `procStart`, so its entries can be neither filtered by the discriminator (E35/RD9) nor liveness-checked against pid reuse (E31) · the fallback parses them and leaves both fields at `UNKNOWN_FIELD`; `is_attached_interactive` therefore refuses them and `discovery_pass` counts them as `skipped_other` rather than registering a session whose liveness it could never check · confirmed live on this host: with the scan pointed at the wrong directory the fallback fired, returned **3** real entries, and all 3 were skipped for exactly this reason · blocks: the fallback is currently discovery-inert — on a machine whose `sessions/` directory does not exist, nothing is registered at all · **evidence gap** — the probe that closes it: run `claude agents --json` beside a live `-p` session **and** a live `cli` session, and record whether any field distinguishes them (the schema capture has only `cli` entries). If none does, the honest fix is to keep the fallback inert and say so in `doctor`.

T7b-3 · the registry's `name`/`nameSource` → `title`/`title_source` mapping (D29's ratchet) **has no carrier**: `FoldDelta` has no `title` or `title_source` field, and `store/` has no title verb, so nothing T7b may touch can persist it · the ratchet is implemented and tested as a pure function (`signals/discovery_loop.py::title_from_registry`, `test_registry_name_sets_title_source`: `user` outranks `engine`, and a later `engine` never overwrites a `user`) but is **not wired to a write** · blocks: the fleet page shows no engine-supplied title on the hookless path, which is the one path that has a title available with no hooks and no transcript read (probe Finding 1) · owner: **T1/T6 — decision gap**
  - options: (a) two fields on `FoldDelta` (`title`, `title_source`) plus the two columns in `DELTA_COLUMNS` — the columns already exist in `session`; (b) a `Store.set_title(session_id, title, source)` verb that applies the ratchet in SQL; (c) leave titles to the hook path only.
  - recommendation (~~**not applied**~~ — **APPLIED**, see below): (a) — the columns exist, the ratchet is written, and `_delta_values` already skips `None`, so it is two dataclass fields and two tuple entries.
  - **APPLIED 2026-09-16 (remediation, cross-cutting owner)** — option (a), unchanged from the recommendation. `FoldDelta` gains `title` and `title_source`; `core/states.py` now owns `TitleSource` and `TITLE_SOURCE_RANK` so the delta and the row are typed by the same alias, and `store/models.py` re-exports it; `DELTA_COLUMNS` gains the two columns, which already existed on `session`. `registry_delta` decides the ratchet once against the prior and carries the pair on every branch — a title is a fact about the session, not about the state it is in. Proven by `tests/signals/test_discovery_loop.py::test_registry_title_reaches_the_session_row` and `::test_stored_user_title_survives_a_later_engine_name` (the ratchet read back out of the store, not out of a hand-built snapshot), and **live against this host, read-only, into a throwaway DB: 3 real sessions, 3 titles, 0 `(untitled)`** — `shepherd-0b`, `aivisor-29` (both `needs_you`) and `shepherd-c8` (`running`), all `title_source = engine`. Before the fix the same run rendered three `(untitled)`.

T7b-4 · `FoldDelta`'s contract is "`None` means *untouched*, never *cleared*", and the registry lane has a **real clearing signal**: the captured pair `06-sidecar-permission.json` → `07-sidecar-running.json` (same pid, 9.194 s apart) is the only observed evidence that a permission-blocked session became unblocked · `needs_you_reason` is therefore cleared with the empty string (`CLEARED_REASON = ""`), which is falsey for every reader and distinguishable from "this delta said nothing" · blocks nothing; recorded because a reader that tests `reason is None` rather than `not reason` will render a stale-looking empty reason · owner: **T11/T14** — if a sentinel type is preferred to `""`, it must be decided once for both lanes.
  - **ANSWERED 2026-09-16, and not with a sentinel type.** `""` stays: it is falsey for every reader, it needs no new type on a frozen dataclass that crosses the store boundary, and a sentinel would have to be taught to `_delta_values`, to SQLite and to every consumer for no behaviour change. What *was* wrong is that the two lanes agreed by coincidence — see T11-6, where the shared constant landed. **What would change this:** a column where the empty string and "cleared" are genuinely different values to a reader. `needs_you_reason` is not one; a future `stop_reason` might be, and that is the decision to reopen this at.

T7b-5 · RD8's recency rule is **only provable at the pure seam**: `Store.snapshot()` returns `observed_at=None` unconditionally (BLOCKER-T6-1 — §7 has no column), so through the store the registry observation always looks newer than the hook's and wins every field · `registry_delta` implements the rule correctly and `test_newer_source_wins_per_field`, `test_equal_timestamps_tie_break_to_the_hook_path` and `test_stale_hook_state_is_corrected_by_the_registry` prove it against a `SessionSnapshot` carrying a real `observed_at` · blocks: F12's freeze-and-discard bug is fixed in the rule but **not yet at runtime** — until `observed_at` is persisted, the two sources resolve by write order, which is the state revision 2 shipped · owner: **T5/T6** — this is the same column BLOCKER-T6-1 already recommends; T7b is its second caller and makes it load-bearing rather than tidy.
  - **CLOSED 2026-09-16 by BLOCKER-T6-1's fix.** `observed_at` is a column; `Store.snapshot()` returns the stored value, so RD8's per-field rule is live at runtime and the two lanes no longer resolve by write order. F12's freeze-and-discard bug is now fixed *at runtime*, not only in the rule.

T7b-6 (found by live proof, **fixed here**) · the first live run of `discovery_pass` against this host registered **zero** sessions: it scanned `HostDirs.config_dir`, which is *Shepherd's* config dir (`/root/.config/shepherd`, ADR-2), while Claude Code writes its sidecars under `$CLAUDE_CONFIG_DIR` or `~/.claude` · `engines/claude_code/registry.py::claude_config_dir()` now owns that path (the engine adapter is where "where the engine keeps its state" belongs) and `discovery_pass`/`run_discovery_loop` take an additive, defaulted `engine_config_dir: Path | None` so a test can point the scan at a throwaway directory · after the fix the same live run found **3 real sessions with no hooks installed**, bound to their repos, two `needs_you` (idle) and one `running` · recorded because the plan's Consumes block names `HostDirs` and could be read as licensing the wrong directory.

T13-1 · `core/anomalies.py` has no member for the two unknowns the subagent rollup actually produces — a **missing agent transcript** (E26: the compaction subagent's announced path never existed) and an **unreadable subagent state** (C20/E28) · `MALFORMED_PAYLOAD` and `UNMAPPED_NOTICE` are reused, each with an explicit detail string naming the agent id, so nothing is dropped or silent · blocks: `doctor`'s anomaly breakdown conflates a torn JSONL line with a subagent whose state simply is not written down anywhere — two conditions with very different meanings · owner: **T1 — `AnomalyKind` is append-only by design**; add `MISSING_SUBAGENT_TRANSCRIPT` and `SUBAGENT_STATE_UNKNOWN` and re-point the two call sites in `engines/claude_code/transcript.py`.
  - **APPLIED 2026-09-16** — `MISSING_SUBAGENT_TRANSCRIPT` and `SUBAGENT_STATE_UNKNOWN` added (`AnomalyKind` is append-only by design), and the two call sites in `engines/claude_code/transcript.py` re-pointed: `_activity_window`'s "does not exist" is the first, and `_agent_state`'s **`status is None`** branch is the second. The other `_agent_state` branch — a status word we *saw* and could not map — stays `UNMAPPED_NOTICE`, which is what it is. `tests/engines/test_transcript_subagents.py` now asserts the two are told apart and that neither is `MALFORMED_PAYLOAD`.

T11-1 · Table A says an internal subagent stop (`agent_type: ""`) is "dropped here, emitting no signal", but C10 also requires that anomaly be **counted**, and `parse_hook_payload`'s declared return type is `Signal | MalformedPayload` — a union with no "nothing" member, and two other builders are writing callers against it right now · **implemented reading:** the *id* is dropped, not the event — the normaliser emits `SUBAGENT_FINISHED` with **no** `subagent_id`, and the fold sees a finish with nothing to pair on, leaves the live-set count untouched and counts one `SUBAGENT_UNDERFLOW`. That is the only reading which satisfies all three of C10's clauses ("ignore `''`, clamp at zero, count the anomaly"), keeps liveness (`last_event_at` still advances on a real event), and leaves the declared signature alone · confirmed against the corpus: 2 starts, 4 stops, **2** counted underflows, `active_subagents` never negative · blocks nothing · owner: **T11 — decision gap for r7**
  - decision: does "dropped" mean the id or the whole event?
  - pressure: dropping the whole event makes C10's "count the anomaly" unimplementable (the normaliser returns no anomalies) and needs a third union member.
  - options: (a) drop the id, emit the signal — what is built; (b) widen the return type to `Signal | MalformedPayload | None` and count nothing; (c) add a `DroppedEvent` value the normaliser returns and every caller must handle.
  - recommendation (**applied**, because (b) and (c) both break callers being written concurrently): (a), and Table A's cell reworded at r7 to say "the **id** is dropped here".
  - **CLOSED 2026-09-16** — the implementation was already option (a) and unchanged; the owed half was the plan's wording, and Table A's `SubagentStop` cell (plan line ~2476) now reads "the **id** of an `agent_type == \"\"` stop is dropped here", with the reason. The entry needed a plan edit, not a code one.

T11-2 · Table B's `TOOL_BATCH_FINISHED` row says it "counts that a refusal happened (counter only at M1)", and the fold cannot: `AnomalyKind` has no member for a refusal, and **nothing in Table A projects a neutral field that says one occurred** — the evidence lives in the batch's `tool_response` text, which is engine vocabulary and M2's heuristic territory · built as `state = running`, clears `needs_you`, no counter · blocks: `doctor` cannot report how often a permission refusal or hook block happened, which is the one thing `PostToolBatch` uniquely records (and `PostToolBatch` is not in `SUBSCRIBED_EVENTS` either, so at M1 nothing dispatches it) · owner: **T1 + T11 — evidence/decision gap.** The probe that closes it: the 40 `PostToolBatch` captures already distinguish a refusal (`tool_response` carries `hook error: … exit 2`) from an ordinary batch — someone must decide the neutral field name and the `AnomalyKind` member from those captures rather than guess. Not applied: `core/anomalies.py` is T1's surface.
  - **APPLIED 2026-09-16, from the captures rather than from a guess.** The 40 `PostToolBatch` payloads were re-read: 32 carry ordinary responses, **1** carries the hook-block marker (`… hook error: … exit 2`, `R01_exit_codes`) and **7** carry a permission sentence. Those are two different facts to whoever reads `doctor` — the user's own hook configuration refused the call, or the engine's approval surface did — so they get two members, `TOOL_BLOCKED_BY_HOOK` and `TOOL_PERMISSION_REFUSED`, not one. The neutral field is `refusal` (added to `SIGNAL_FIELD_KEYS`), with a closed two-value vocabulary in `core/signals.py`. **The classifier is literal, not semantic:** `normalise.py` matches the engine's own marker and its four captured refusal-sentence prefixes; reading prose for meaning is M2's heuristic territory, so a wording we have not captured projects nothing and the counter **under-counts rather than inventing a refusal**. `PostToolBatch` still is not in `SUBSCRIBED_EVENTS` — plan gap M7 says it should be, and that is T9/T18's surface, not this one; until it is subscribed the counter is correct and idle.

T11-3 · `repos_touched` cannot be computed for a changed path **outside** the session's own cwd: resolving an arbitrary path to a repo runs git, and `fold()` is pure by contract · built rule: a changed path inside `prior.cwd` marks `prior.repo_id` touched; a path elsewhere is left alone rather than guessed at (principle 5) · this is visible in the corpus — the one `Write` capture writes to `/tmp/shp-hooks-tui-victim-…/outside.txt` from a session whose cwd is elsewhere, which is exactly the cross-repo case the column exists for, and it is not attributed · blocks: cross-repo `repos_touched` is empty at M1 · owner: **T11/T14 — decision gap**
  - options: (a) the caller (the hook lane's composition root) resolves unattributed paths through `bind_cwd_to_repo` and applies a second `FoldDelta(repos_touched=…)`, exactly as the replay harness already does for a cwd change; (b) a store verb `repo_for_path(path)` over `repo.root_path`, pure SQL and no git; (c) leave it.
  - recommendation (**still not applied**, and deliberately — see the disposition below): (b) — it needs no subprocess, it is one verb, and every repo the fleet knows about already has a `root_path`.
  - **NOT APPLIED 2026-09-16, deliberately, and the entry stays open.** Recommendation (b) — a `repo_for_path` store verb — is *correct and insufficient*: `fold()` is pure by contract and cannot call the store, so the verb alone closes nothing. The path that actually works is (a): the hook lane's composition root resolves unattributed paths and applies a second `FoldDelta(repos_touched=…)`, exactly as the replay harness already does for a cwd change. That composition root is **T18**, it does not exist yet, and adding a verb with no caller would be an abstraction with one hypothetical user. Meanwhile the built behaviour is honest under principle 5: a path outside the session's own cwd is left unattributed rather than guessed at. **What would change this:** T18 landing, or a second caller for the verb appearing first. Cross-repo `repos_touched` is empty at M1 and `doctor` should say so.

T11-4 (confirms BLOCKER-T6-1) · the fold's identity sets (live subagent ids, created/completed task ids) have no column, so `Store.snapshot()` returns them empty every time — meaning a fold driven purely off the store can never pair a subagent stop with its start, and `tasks_done` can never be computed as an intersection · `FoldResult` therefore carries a fourth field, `sets: IdentitySets`, and `signals/fold.py::advance(prior, result)` builds the caller's in-memory successor snapshot; the golden lane carries them in a dict and proves the pairing works · blocks: **a daemon restart silently resets every session's subagent pairing and task sets** — after a restart an in-flight subagent's stop becomes an unmatched stop and is counted as an anomaly that is not one · owner: **T5/T6**, same column family as BLOCKER-T6-1 and T7b-5. Recommendation (~~**not applied**~~ — **APPLIED**, see below, as three columns rather than one): persist the three sets as one JSON column on `session` (or an `app_state` row per session), written by the same `apply_fold_delta` call that writes the counts.
  - **CLOSED 2026-09-16 by BLOCKER-T6-1's fix**, by the entry's own recommendation and then some: the three sets are persisted (three columns rather than one, so each is legible on its own), written by the same `apply_fold_delta` call that writes the counts. `FoldResult.sets` and `fold.advance()` stay — they are no longer a workaround but the pure lane's own path, which T12's replay of 429 events uses with no store at all.

T11-5 · `FoldRule` as built carries three columns the plan's Produces block does not name — `sets_fn`, `anomaly_fn` and `clears_needs_you` · each exists because a cross-cutting rule needs a per-kind fact and the alternative was a second table keyed on `SignalKind` beside the first (which is the exact defect r5 split the tables to remove): `clears_needs_you` **is** Table B's "Clears `needs_you`?" column made machine-readable, `sets_fn` is how a rule's count stays `len(set)` rather than ±1 (C10), and `anomaly_fn` is how principle 5's counting stays one row per kind · `delta_fn`, `emits`, `evidence` and `kind` are exactly as specified, and `test_every_signal_kind_has_exactly_one_rule` still polices totality · blocks nothing · owner: T11 — recorded so the widening is not later found as undocumented drift.
  - **CORRECT AS BUILT 2026-09-16; no change, and the entry stays as the record of the widening.** Each of the three columns is a per-kind fact a cross-cutting rule needs, and the only alternative is a second table keyed on `SignalKind` beside the first — which is the exact defect r5 split the tables to remove. `clears_needs_you` is Table B's own "Clears `needs_you`?" column made machine-readable; `sets_fn` is how a count stays `len(set)` rather than ±1 (C10); `anomaly_fn` is how principle 5's counting stays one row per kind, and this remediation added a second user of it (`TOOL_BATCH_FINISHED`'s refusal counter, T11-2) — which is the test of whether the column earned its place, and it did. `test_every_signal_kind_has_exactly_one_rule` still polices totality. **What would change this:** a rule needing a fourth per-kind function, which would be the signal that the row has become a class and should be one.

T11-6 · the inferred `needs_you` clear writes the **empty string**, not `NULL`: `FoldDelta`'s contract is "`None` means untouched, never cleared" (`store/db.py::_delta_values` skips `None`), so a clear needs a value · `signals/fold.py::CLEARED = ""`, which is falsey for every reader — and it is the **same choice T7b-4 made independently for the registry lane** (`CLEARED_REASON = ""`), so both lanes already agree · blocks: a reader testing `reason is None` rather than `not reason` renders a stale-looking empty reason · owner: **T11/T14** — T7b-4's open question ("if a sentinel type is preferred to `''`, it must be decided once for both lanes") is now answered consistently in both, but by convention rather than by a shared constant. Recommendation (~~**not applied**~~ — **APPLIED**, see below): one `CLEARED` constant in `core/fold_types.py` that both lanes import.
  - **APPLIED 2026-09-16** — one `CLEARED` constant in `core/fold_types.py`, imported by `signals/fold.py` (as `CLEARED`) and by `signals/discovery_loop.py` (as `CLEARED_REASON`, its existing name, now bound to the shared constant). Agreeing by convention is not agreeing by construction; asserted by `tests/test_core_types.py::test_both_fold_lanes_return_the_same_type_and_the_same_clear`.

T12-1 · the golden lane lives under `tests/golden/` (loader, replay harness, expected table and both test modules) rather than the plan's `tests/fixtures/corpus.py` + `tests/signals/test_fold_*.py` — the build brief scoped this builder to `tests/signals/` and `tests/golden/`, and two other builders were writing `tests/` concurrently · `tests/golden/` is a **package** (`__init__.py`): without it, prepend import mode gave its `conftest.py` the top-level module name `conftest`, which shadowed `tests/signals/conftest.py` and broke `from conftest import GitWorld` in T7's `test_binding.py` — caught and fixed in this phase · the module names the plan specifies (`test_fold_golden_corpus.py`, `test_fold_concurrent_interleave.py`) are unchanged · blocks nothing · owner: T12 — recorded as a path deviation, not a behaviour one.
  - **CORRECT AS BUILT 2026-09-16; no change.** This is a path deviation with a reason that has not expired: `tests/golden/` is a package precisely because prepend import mode gave its `conftest.py` the top-level name `conftest` and shadowed `tests/signals/conftest.py`, breaking T7's `test_binding.py`. Moving the loader to `tests/fixtures/corpus.py` now would re-litigate that for no behavioural gain, and the two module names the plan specifies are unchanged. **What would change this:** the plan's own path being load-bearing for a later task — nothing in T13–T18 reads it.

T12-2 · `Store.register_session` mints `session.id` with `new_ulid()` — the **wall-clock lane**, no injected `now` — so the ulid is the one value in a replayed row that cannot be byte-identical run to run · the golden table is therefore keyed on the **engine's** `session_id` and stores repo **root paths** rather than repo ids; `test_replay_is_idempotent` and `test_fold_concurrent_interleave` compare those tables and pass · this is the same observation as the plan's filed note that golden fixtures should assert decoded time prefixes rather than rely on call order · blocks nothing today; it blocks any future golden assertion on `session.id` itself · owner: **T6** — recommendation (~~**not applied**~~ — **APPLIED**, see below): give `register_session` an optional `now: float | None` that reaches `new_ulid(now=…)`, so a replay can mint ids from `_epoch` like everything else in the lane.
  - **APPLIED 2026-09-16** — `register_session(..., now: float | None = None)` reaches `new_ulid(now)`. Omitted it is the wall-clock lane, which is what every live caller wants; injected it is byte-identical across processes. `tests/store/test_verbs.py::test_register_session_mints_its_id_from_an_injected_clock` asserts the **decoded** 10-character time prefix against `01M2G8CJX7` — Crockford base32 of a real captured `_epoch`, encoded independently of `core/ids.py` — rather than against `new_ulid`'s own output. The golden table is not re-keyed here: that is T12's call to make now that it can.

T14-1 · `subscribe()` cannot return `Iterator[StreamEvent]` and still satisfy §12 · the plan's Produces block types it that way, but §12's contract (`Last-Event-ID` gap replay, monotonic `seq`, `test_subscribe_replays_from_seq`, T15's `test_sse_seq_monotonic`) is entirely about the **seq**, and `core.stream.StreamEvent` (T1) has no field for one — adding one is `core/`, which is another builder's surface this phase · tried: carrying `seq` inside `payload` (it belongs to the producer, and a reader could not distinguish ours from theirs) · built: `toolsurface/stream.py` defines `@dataclass(frozen=True) class StreamDelivery: seq: int; event: StreamEvent`, and `subscribe(since_seq, idle_timeout_s=None) -> Iterator[StreamDelivery]` · blocks: **T15 must consume `StreamDelivery`, not a bare `StreamEvent`** — `delivery.seq` is the SSE `id:` and `delivery.event` is the envelope · owner: T14/T15 — recorded so the plan's Produces block is corrected rather than the code re-derived from it.

T14-2 · D38.1 (given in this phase's brief, not yet in the plan file) adds three tools beyond Task 14's "exactly five" · `install_hooks` / `uninstall_hooks` / `inspect_hooks` are registered in `toolsurface/tools_hooks.py` with `blast_class=LOCAL_DESTRUCTIVE` and `audiences={HUMAN}`, closing the **open decision gap recorded against Task 17** (`cli/` may not import `engines/`, and the write commands had no legal path): this is candidate (c) of the three, narrowed — the installer trio becomes M1 write tools, gated at M4 by one policy table rather than by finding them · blocks nothing; T17 calls them through `invoke()` · owner: plan revision — Task 14's "Allowed Scope: exactly five `ToolDef`s" and Task 17's OPEN block both need amending to match.

T14-3 · `fleet_summary` reports anomaly counts by asking `Store.get_app_state("anomaly.<kind>")` once per `AnomalyKind`, because `store/` has no verb that *enumerates* the `anomaly.*` keys · tried: nothing else is possible from outside `store/`, and `store/` is another builder's surface this phase · consequence: an anomaly kind counted under a key that is not an `AnomalyKind` member would be invisible to the fleet page · blocks nothing today (`bump_anomaly` is only ever called with a member) · owner: **T6** — recommendation (**not applied**): a `list_anomaly_counts() -> dict[str, int]` verb, so the reader enumerates what was actually counted rather than what it expected to be counted.
  - **APPLIED 2026-09-16 (picked up by the `core/`+`store/` remediation, which owns `store/` this phase).** `Store.list_anomaly_counts() -> dict[str, int]` enumerates every `anomaly.*` key under a named `ANOMALY_KEY_PREFIX` and returns `{kind: count}`. This became load-bearing in the same phase: the remediation added four `AnomalyKind` members (T13-1, T11-2), and a reader that asks only for the members it knows would have shown a zero for a refusal that really happened. `tests/store/test_verbs.py::test_anomaly_counts_are_enumerated_not_guessed_at` proves it also ignores non-anomaly `app_state` keys. T14 may switch `fleet_summary` to it; the per-key reads still work.

T14-4 · `src/shepherd/store/db.py` is **607 lines** and fails `tests/boundaries/test_composition_root.py::test_no_source_file_exceeds_600_lines` (K4) · observed at the end of T14's build; `store/` is the concurrent remediation builder's surface and T14 touched no file in it · blocks: the boundary lane is red until `db.py` is split or trimmed · owner: **the `core/`+`store/` remediation phase**, not T14.
  - **FIXED 2026-09-16 by the remediation, not by trimming.** `db.py` grew past the cap because BLOCKER-T6-1's four columns landed in it; the split is `store/rows.py`, which now owns driver row → dataclass (D33 rule 2's whole content) and leaves `db.py` the verbs and the SQL. `db.py` is 535 lines, `rows.py` 129, and `test_no_source_file_exceeds_600_lines` is green.

T17-1 · `doctor` cannot report the schema version, so §7 migration rules 1–2 have no line of their own · `read_schema_version` lives in `store/migrate.py` (L1) and `cli/` is L5: `tests/boundaries/test_consumer_boundary.py` lists `shepherd.store` in `FORBIDDEN_BELOW_L4`, and no registered tool projects a schema version — the plan's Task 17 `Consumes` block names `read_schema_version` (T5), which the same rule that killed `scan_registry` also forbids · built instead: `doctor`'s `database:` line reports the **data directory** state from `HostPlatform.dirs()` — `not created yet — nothing at <data_dir>, so the control daemon has never run on this host` (F16), and otherwise says the schema version is unknown **and why**, rather than printing a version it guessed · blocks: nothing downstream; it is a missing fact, printed as a missing fact (principle 5) · owner: **T14/toolsurface** — add a `schema_status` `ToolDef` (`local_read`, `{HUMAN, MASTER}`) projecting `{schema_version, expected_version, database_exists}` from `read_schema_version`, and `doctor` prints it with a one-line change. **The plan's Task 17 `Consumes` block needs the same amendment it already made for `scan_registry`.**

T17-2 · `doctor` cannot report the engine version or the drift-check date (G12) · the line the plan specifies (`engine: 2.1.273 · schemas pinned 2.1.270 · names+enums verified <date>`) needs `claude --version` plus the recorded drift-check result; both are engine knowledge, `cli/` may not import `engines/` (D19, D35), and running the engine binary from an L5 consumer would put the engine's vocabulary in the one layer §5.0 exists to keep free of it · built instead: `engine: unknown — no registered tool reports the engine version or the schema drift-check date (T17 blocker…)`, asserted by `test_doctor_reports_the_engine_version_as_unknown` so the gap cannot be quietly closed by rendering a blank · blocks: G12's "stated on every run, not buried in a probe file" · owner: **T14/toolsurface** — an `engine_version` `ToolDef` (`local_read`) returning `{version, pinned_version, drift_checked_at}`.

T17-3 · the `shepherd` console entry point is not registered · `main(argv) -> int` and `run()` exist in `src/shepherd/cli/main.py` and are exercised by all 27 tests in `tests/cli/`, but the `[project.scripts]` table lives in `pyproject.toml`, which is outside the surface this build was scoped to while two other builders were writing concurrently · blocks: `shepherd` on `PATH`; nothing importable · owner: **T18 wiring** — add `[project.scripts] shepherd = "shepherd.cli.main:run"`. T18 also owns the other half: `cli/` reaches capabilities through `invoke()`, so a `shepherd` process needs the composition root to have registered the tools — with an empty registry `status` exits 1 and says so (`test_status_fails_when_no_tool_is_registered`), which is the honest behaviour but not yet the useful one.

T17-4 · `status` cannot say **which source saw each session** (registry vs hooks) row by row · `project_session` in `toolsurface/tools_m1.py` whitelists seventeen fields and a per-row discovery source is not among them; `cli/` reads only what a tool projects (§13) · built instead: the fleet-level answer on `doctor`'s `discovery:` line — `hooks <state> · registry N sessions · …` — which is Decision pressure 5's requirement, but not the per-row one · blocks: nothing; both sources already converge on one row, so the row is correct either way · owner: **T14/toolsurface** (with T6 if the column is not stored) — add `discovery_source` to `project_session`, and `status` prints it with a one-line change.

T16-1 · the fleet page cannot order its rows by state, because **no tool projects an ordered fleet** · §16 and the phase brief both require "ordering derived from state, never alphabetical and never by mtime", using `signals/ordering.fleet_sort_key` rather than re-deriving it in JavaScript — but of the five read tools, `list_sessions` returns rows ordered by SQL `started_at, id` with the **raw** `state` column (no `effective_state` backstop), and `fleet_summary`, which *does* apply `effective_state` over `Store.fleet()` (already `_fleet_order_clause()`-ordered), returns only **counts** and the `needs_you` list — not the rows · tried: (a) sorting in `web/` from the projected `state` string — that is re-deriving the order in a consumer, which is exactly what the brief forbids, and it still could not apply §8's read-time demotion, which needs `last_event_at` compared against `LIVENESS_WINDOW_S` inside L2; (b) importing `fleet_sort_key` — `shepherd.signals` is in `FORBIDDEN_BELOW_L4` and `tests/boundaries/test_consumer_boundary.py` fails the build on it, correctly · built instead: the page renders **the order the API handed it** and never sorts (`tests/web/test_fleet_page.py::test_the_page_does_not_re_derive_the_order` asserts `.sort(`, `localeCompare` and `FLEET_STATE_ORDER` are all absent from `fleet.js`), so the day the tool arrives the page is correct with no JS change · blocks: §16's ordering on the fleet page — at M1 sessions appear in creation order within each workspace, and a `needs_you` session does not float to the top · owner: **T14/toolsurface** — recommendation (**not applied**): add a `fleet_tree` `ToolDef` (`local_read`, `{HUMAN, MASTER}`) projecting `Store.fleet()` rows through `effective_state` and `sorted(..., key=fleet_sort_key)` — the three ingredients already exist and already sit together in `tools_m1.fleet_summary`; the tool is a projection, not new logic. `tests/web/test_fleet_page.py::test_fleet_ordering` already pins the expected order against `fleet_sort_key` directly, so the tool has its assertion waiting.

T15-1 · a **tail** SSE subscriber has a one-instant window in which it can miss an event · `subscribe(since_seq=None)` starts at the ring's current tail and, with an `idle_timeout_s`, ends when nothing has arrived; `web/sse.py` then re-subscribes to keep the connection open, and until the first event arrives it has no seq to resume from, so it must pass `None` again — an event published between the two calls is skipped · tried: resuming from `0` instead (that replays the whole ring to every idle tab, which is worse), and holding one generator open forever (`subscribe()` returns on idle by design, and the alternative — `idle_timeout_s=None` — returns as soon as the backlog drains) · scope: the window is microseconds and only exists **before the first event of a connection**; every subsequent reconnect carries `Last-Event-ID`, which is the contract §12 actually specifies and which is tested (`test_sse_replay_from_last_event_id`, `test_sse_replay_gap_is_explicit`) · blocks: nothing observable today; it is a correctness hole in the "no polling, never miss" claim · owner: **T14/toolsurface** — recommendation (**not applied**): expose the ring's current seq as a datum (a `stream_cursor` field on `fleet_summary`, or a `current_seq()` alongside `subscribe`), so the page's first connection resumes from a **number** and `web/` never needs the `None` cursor at all. The comment at the bottom of `sse.stream()` names this blocker so the next reader does not rediscover it.

T16-2 · `static/` is not declared as package data · `src/shepherd/web/static/{index.html,app.css,app.js,fleet.js,sse.js,escape.js}` are served from the package by path (`create_server(..., static_root=...)`), which works from a source tree and in the tests, but `pyproject.toml`'s `[tool.setuptools.packages.find]` ships `.py` files only — an installed wheel would answer `/` with a 404 · tried: nothing — `pyproject.toml` is outside this phase's surface (`src/shepherd/web/`, `tests/web/`) and two builders were writing concurrently · blocks: `pip install shepherd` serving the fleet page; not the repo-run path · owner: **T18 wiring** — add `[tool.setuptools.package-data] "shepherd.web" = ["static/*"]` (alongside T17-3's `[project.scripts]`, same file, same edit).

---

## T18 ledger sweep — 2026-09-16, the last builder in M1

Every entry above is dispositioned below: **APPLIED**, **CLOSED**, **CORRECT AS
BUILT**, or **NOT APPLIED with what would change it**. An entry that quietly
disappears is worse than one that stays open, so nothing is deleted and nothing
is silently downgraded. Evidence for every "APPLIED" is a named test that fails
without the change.

Baseline this sweep started from: `pytest tests` → 405 passed, 1 skipped (with
`test_reads_use_a_thread_local_connection` deselected); `mypy --strict
src/shepherd` → clean, 56 files. Baseline it ends at: **443 passed, 1 skipped, 8
deselected** on `pytest -m "not live"`, **7 passed** on `pytest tests/e2e -m live`,
`mypy --strict` clean over **61 files**, and `~/.claude/settings.json` sha256
`375e5322…` byte-identical before and after every run.

| Entry | Disposition |
|---|---|
| **T3/T4 — macOS `MacHost` unverified (G1)** | **OPEN BY DESIGN, unchanged.** `MacHost` ships unverified (D55) and `verified()` is False; that is the expected state at M1, not a failure. This sweep *added* one more unverified macOS value — `host/mac.py::peercred()` returns `None` with the `UNVERIFIED (no capture)` marker, because macOS spells peer credentials `LOCAL_PEERCRED`/`LOCAL_PEERPID` over `SOL_LOCAL` and no capture of either exists (D41 forbids typing a shape from memory). Closing it is still the one macOS run this entry already enumerates, now with peer credentials added to the capture list. |
| **BLOCKER-T6-1 — fold identity columns** | **CLOSED** (applied in the earlier remediation, evidence unchanged: `tests/store/test_verbs.py::test_snapshot_roundtrips_the_fold_identity_fields`, `tests/signals/test_fold_rules.py::test_subagent_pairing_survives_a_controld_restart`). T18 depends on it: `HookLane` holds no state between frames and reads its prior back out of the database, which is only correct because those columns exist. |
| **T5/T6/T7 — `repo.git_common_dir` widening** | **CORRECT AS BUILT.** D48 replaced longest-prefix matching and the binding key has to live somewhere; T18 is now its third caller (`HookLane` cross-repo attribution resolves a foreign path to a `repo` row through exactly this key — `tests/signals/test_hook_lane.py::test_a_changed_path_in_another_repo_is_attributed` asserts the row it finds is the one `find_repo_by_common_dir` returns). **What would change this:** a reviewer rejecting the column would also have to reject `find_repo_by_common_dir` and the cross-repo fix built on it. |
| **T11 (from T2 r6) — `Signal.fields` fails closed on whole-mapping reads** | **CORRECT AS BUILT, and load-bearing this phase.** T18's hook lane needed the changed paths and could not read `.fields` as a mapping; it reads the one named neutral key through `signals/rules.py::changed_paths` (promoted from `_changed_paths` — same body, now a named public helper with two callers). That is the rule working as designed rather than an exemption being added. **What would change this:** a fold genuinely needing the whole mapping, which is a plan change and not an exemption. |
| **T12 (from T2 r6, C1) — `new_ulid(now=…)` is authoritative** | **CORRECT AS BUILT.** Every live call site still uses the no-argument wall-clock lane; the injected lane is used only by golden fixtures and by `register_session(now=…)` (T12-2). Nothing in T18 injects a clock. |
| **BLOCKER-T6-2 — `test_reads_use_a_thread_local_connection` flaked on recycled idents** | **CLOSED, and verified this phase rather than assumed.** The test now keys `seen` by a caller-supplied index and uses a `threading.Barrier` so all four workers are alive at once — both halves the entry asked for. Proven here: three consecutive full runs **with the test selected** (`pytest tests -m "not live"` → 443 passed, 1 skipped, three times), plus the exact ordering that used to fail (`pytest tests/contracts tests/store/test_verbs.py::test_reads_use_a_thread_local_connection` → 20 passed, 1 skipped). **The habitual `--deselect` is no longer needed.** |
| **T9 — `inspect_hooks(settings_path, entry=None)` widening** | **CORRECT AS BUILT.** The additive parameter stands, and with no `entry` the field is False ("unknown is not 'matches'"). T18 does not call it with an entry: `controld` registers `inspect_hooks` through `register_hook_tools(entry=build_hook_entry(...))`, so the composition root supplies the host's command exactly where the entry said it belongs. **What would change this:** moving `detect_host()` inside the inspector, which is the alternative the entry already rejected. |
| **T10 — `SO_PEERCRED` has no home** | **APPLIED — option (c), the entry's own recommendation.** `host/linux.py::peercred(sock)` reads `struct ucred` via `SO_PEERCRED`; `host/mac.py::peercred(sock)` returns `None` and says why (G1); `host/detect.py::detect_peercred()` chooses between them, so the platform branch stays in the one package that may hold it and `HostPlatform` stays at D55's six members. `sessiond` injects it into the `peer_credentials` hook `serve_ingest` already had. Evidence: `tests/daemons/test_peercred.py` — five tests, including a real `AF_UNIX` socket pair, and `Frame.peer_pid == os.getpid()` end-to-end through `serve_ingest` with a real client connecting. E21 is unchanged: the frame's own `CLAUDE_PID` stays authoritative and this is the spoof-resistant second opinion. |
| **T7b-1 — two `FoldResult` types** | **CLOSED** (applied earlier). T18 is the caller the entry predicted: `controld`'s `publish_result(result: FoldResult)` names exactly one type and both lanes reach it. |
| **T7b-2 — `claude agents --json` is a projection** | **NOT APPLIED — still an evidence gap, and the fallback stays deliberately inert.** No probe was run here, so nothing was decided from a guess: the fallback still parses entries whose `entrypoint` and `procStart` are `UNKNOWN_FIELD`, `is_attached_interactive` still refuses them, and `discovery_pass` still counts them as `skipped_other`. `doctor`'s `discovery:` line prints that count, so the inertness is visible rather than silent. **What would change it:** the probe the entry names — `claude agents --json` beside a live `-p` session *and* a live `cli` session, recording whether any field distinguishes them. If none does, the honest fix is to keep the fallback inert and say so in `doctor`'s text as well as its counters. |
| **T7b-3 — registry title ratchet had no carrier** | **CLOSED, and re-proven live in this phase.** `controld` against this host's real registry rendered three real sessions with three real titles and no `(untitled)`: `needs_you shepherd-0b`, `starting shepherd-c8` (both `/root/Shepherd`) and `needs_you aivisor-29` (`/root/AIVisor`). |
| **T7b-4 / T11-6 — the cleared-reason sentinel** | **CLOSED.** One `CLEARED` constant in `core/fold_types.py`, imported by both lanes. Unchanged here; T18 adds no third lane. |
| **T7b-5 — RD8 only provable at the pure seam** | **CLOSED** by BLOCKER-T6-1's `observed_at` column. |
| **T7b-6 — the scan read Shepherd's config dir, not the engine's** | **CORRECT AS BUILT, and re-proven.** `controld` passes `engine_config_dir=None` in production, which means `claude_config_dir()` — the engine's — and pointed there it found this host's three real sessions. The tests pass a throwaway directory through the same parameter, so the deterministic lane and the live lane differ by one argument and nothing else. |
| **T13-1 — no anomaly member for a missing subagent transcript** | **CLOSED** (applied earlier: `MISSING_SUBAGENT_TRANSCRIPT`, `SUBAGENT_STATE_UNKNOWN`). |
| **T11-1 — "dropped" meant the id, not the event** | **CLOSED** (implementation unchanged; the plan's Table A wording was the owed half). |
| **T11-2 — refusal counter had no member and no field** | **CLOSED** (applied from the 40 captures: `TOOL_BLOCKED_BY_HOOK`, `TOOL_PERMISSION_REFUSED`, neutral field `refusal`). Still idle because `PostToolBatch` is not in `SUBSCRIBED_EVENTS` (plan gap M7) — see the new entry T18-2 below. |
| **T11-3 — cross-repo `repos_touched`** | **APPLIED — by path (a), at the composition root the entry named.** The entry's own note said recommendation (b) (a `repo_for_path` store verb) was "correct and insufficient" because `fold()` is pure and cannot call the store, and that the real fix was (a): the hook lane's composition root resolves unattributed paths and applies the result. That composition root now exists. `signals/hook_lane.py::HookLane._with_foreign_repos` takes the changed paths the pure fold left unattributed, resolves each *directory* through the same `bind_cwd_to_repo` the registration uses, and carries the result on the same `FoldDelta` — one writer, one rule set (D37). No store verb was added: (b) would have been an abstraction with one hypothetical caller, and the caller it would have had does not need it. Evidence: `tests/signals/test_hook_lane.py::test_a_changed_path_in_another_repo_is_attributed` (the write lands in a *different* real git repo and that repo's row is the one `find_repo_by_common_dir` returns), and `::test_an_unattributable_path_is_left_alone_not_guessed_at` (a path in no repo adds nothing — principle 5 is preserved, not traded away). **Note on semantics, stated because it is easy to misread:** §7's column is "repo ids **accumulated**" from the paths that changed, so a write that lands only outside the session's own repo marks only the other repo — the session's own repo is not marked by an event that did not touch it. **Citation corrected (independent verification, 2026-09-17):** this note previously cited C9 for that rule. C9 rules only on the *source* — `FileChanged` never fires for agent edits, so `PostToolUse` paths are read instead — and makes no ruling about which repos get marked. The rule comes from §7's `repos_touched` column and the worked `session` row that spans two repos ("it started in `billing-api` and has since edited `payments-api` too"). The same miscitation in the comment at `tests/signals/test_hook_lane.py` is corrected too. **Coverage gap closed at the same time:** the spec's own accumulation example (`('A',)` → `('A','B')`) had no test. `tests/signals/test_hook_lane.py::test_repos_touched_accumulates_across_two_repos` now drives two events into two real git repos and asserts the second does not replace the first; a mutation that drops `prior.repos_touched` fails it and fails nothing else. |
| **T11-4 — identity sets had no column** | **CLOSED** by BLOCKER-T6-1's fix. |
| **T11-5 — `FoldRule`'s three extra columns** | **CORRECT AS BUILT.** Unchanged; `sets_fn`/`anomaly_fn`/`clears_needs_you` each still have ≥2 users. |
| **T12-1 — the golden lane's path deviation** | **CORRECT AS BUILT.** Nothing in T13–T18 reads the plan's path, as the entry predicted. |
| **T12-2 — `register_session` and the wall-clock ulid** | **CLOSED** (`now: float | None = None` applied). T18's live and deterministic lanes both use the wall-clock lane, which is what every live caller wants. |
| **T14-1 — `subscribe()` returns `StreamDelivery`, not `StreamEvent`** | **CORRECT AS BUILT, with a plan correction still owed.** The seq belongs to the ring, `core.stream.StreamEvent` has no field for one, and `web/sse.py` consumes `delivery.seq` as the SSE `id:`. Proven live in this phase: a subscriber's first frame carried `id: <n>` and an envelope whose `seq` equalled it (`tests/daemons/test_controld.py::test_a_relayed_frame_becomes_a_row_and_an_sse_update`). **What is owed:** Task 14's Produces block in the plan file, which still types `subscribe()` as `Iterator[StreamEvent]`. That is a plan edit, not a code one. |
| **T14-2 — D38.1's three tools beyond "exactly five"** | **CORRECT AS BUILT, and the plan amendment owed is now larger.** T18 registers **eleven** tools, not five, and the list was read off a running `controld` rather than counted from the source: `engine_version`, `fleet_summary`, `fleet_tree`, `get_session`, `inspect_hooks`, `install_hooks`, `list_projects`, `list_sessions`, `list_subagents`, `schema_status`, `uninstall_hooks` — six read tools (T16-1 added `fleet_tree`), D38.1's installer trio, and T17's two. Each is a `ToolDef` behind `invoke()` with a declared `blast_class` and audience, which is the property Task 14's "exactly five" was protecting; the number was never the invariant. **What is owed:** Task 14's Allowed Scope and Task 17's OPEN block, amended to the list this build actually registers. |
| **T14-3 — `fleet_summary` asked per `AnomalyKind` member** | **APPLIED here, closing the entry's second half.** The verb (`Store.list_anomaly_counts()`) landed earlier; the *reader* still enumerated members, so a kind counted under a key this build has no member for was invisible to the page. `fleet_summary` now unions "every kind this build knows, at zero" with what was actually counted. Evidence: `tests/toolsurface/test_tools_m1.py::test_fleet_summary_shows_an_anomaly_it_did_not_expect`, which counts `a_kind_from_a_newer_build` and asserts it reaches the page. |
| **T14-4 — `db.py` was 607 lines** | **CLOSED** (split into `store/rows.py`). Re-checked after the 2026-09-17 remediation: no source file exceeds 600 lines and no `daemons/` file exceeds 150 — `controld.py` is **148**, with headroom restored by T18-3's fix below. |
| **T17-1 — `doctor` could not report the schema version** | **APPLIED — `schema_status`, exactly as the entry specified.** `toolsurface/tools_engine.py` projects `{schema_version, expected_version, database_exists}` from `store/migrate.read_schema_version`; `cli/commands.describe_database` prints it, and a version mismatch is a `doctor` problem (non-zero exit), not a printed curiosity. The consumer boundary is untouched: `cli/` still imports nothing below L4. Evidence: `tests/toolsurface/test_tools_engine.py` (4 tests) and `tests/cli/test_commands.py::test_doctor_reports_the_schema_version_when_a_tool_projects_it`. |
| **T17-2 — `doctor` could not report the engine version or the drift date** | **APPLIED — `engine_version`, and the drift check is a real gate.** `engines/claude_code/version.py` discovers the running version with `claude --version` (no `shell=True`, bounded timeout, `None` when there is no answer) and reads a drift record; `docs/probes/drift_check.py --record <path>` now writes one. G12's line is live: `engine: 2.1.273 · schemas pinned 2.1.270 · drift check: never run on this host` — a real version beside a real, *honest* gap. **Principle 5 is the load-bearing part:** an absent or torn record reads as `unchecked`, never as `clean`; a verdict of `drift` is a `doctor` problem that exits non-zero. The gate itself was run live this phase: `drift_check.py --record …` → **exit 0**, engine 2.1.273 discovered (not hard-coded), 33/33 event names present, all four enums exact and unchanged. |
| **T17-3 — no `shepherd` console entry point** | **APPLIED, both halves.** `[project.scripts]` registers `shepherd`, `shepherd-controld` and `shepherd-sessiond`, each asserted to resolve to a real callable (`tests/test_packaging.py::test_every_console_command_resolves_to_a_real_callable` — a manifest string is not an entry point until it imports). The entry's *second* half — "a `shepherd` process needs the composition root to have registered the tools" — is closed for the two facts that need no store: `cli/main.register_local_reads` registers `schema_status` and `engine_version` for its own process **only when the registry is empty**, so a process composed by someone else keeps exactly that surface. Nothing in `cli/` opens the store or migrates (§7 rule 3). Evidence: `tests/cli/test_commands.py::test_doctor_in_a_standalone_process_still_reports_real_facts`, and a real run of **`python -m shepherd.cli.main doctor`** on this host printing the engine line above. **Correction (independent verification, 2026-09-17):** this entry previously claimed "a real run of `shepherd doctor` on this host". That is not reproducible and was not what was proven — `shepherd` is not on `PATH` here and nothing named `shepherd*` is installed; the package is run from `src/` via `pythonpath`. `[project.scripts]` is correct and `test_every_console_command_resolves_to_a_real_callable` is honest, but it asserts **importability of the entry point, not installedness of the command**. What was actually proven is the module run above — which itself only started working once T18-5 below added the `__main__` guard. **What is still true:** `shepherd status` against a *running* daemon still exits 1 and says so, because the fleet lives in the daemon's process and there is no transport between them — see T18-1 below. |
| **T17-4 — `status` cannot say which source saw each row** | **NOT APPLIED.** `project_session`'s whitelist still has no per-row discovery source, and `session` has no column for one, so closing it is a store column plus a projection field plus a fold rule deciding what to write when both lanes see the same row — three surfaces, none of them named by any M1 task. The fleet-level answer (`doctor`'s `discovery:` line) is Decision pressure 5's actual requirement and is shipped. **What would change it:** a `discovery_source` column on `session` written by whichever lane registered the row, decided together with RD8's per-field recency (a row *both* lanes touch has no single source, and a column that says "registry" for a row the hook path last wrote would be worse than no column). |
| **T16-1 — the fleet page cannot order by state** | **APPLIED — `fleet_tree`, exactly the recommendation.** `toolsurface/tools_m1.fleet_tree` projects `Store.fleet()` rows through `effective_state` and `sorted(..., key=fleet_sort_key)`, grouped workspace → session, with workspaces ordered by their best-ranked session. It is a projection, not new logic: all three ingredients already existed. The page reads `/api/fleet/tree` and still never sorts — `test_the_page_does_not_re_derive_the_order` is unchanged and still green, and `groupByWorkspace` is gone from `fleet.js` because the grouping now arrives with the payload. Evidence: `tests/toolsurface/test_tools_m1.py::test_fleet_tree_orders_by_state_not_by_creation` (rows created stopped-first, so creation order and state order disagree), `::test_fleet_tree_applies_the_read_time_liveness_backstop` (§8's demotion, the half `web/` could never do for itself), and over HTTP in `tests/web/test_fleet_page.py::test_the_fleet_route_hands_the_page_an_ordered_tree`. Live: a `needs_you` session was the first row on the page. |
| **T15-1 — a tail SSE subscriber's one-instant window** | **NOT APPLIED, and the entry stays open.** The recommendation (expose the ring's current seq as a datum so the page's first connection resumes from a number) is right and was not built here, because it is not one change: it needs `current_seq()` in L4, a `stream_cursor` field on `fleet_summary`, and — the part that actually closes the hole — `sse.js` carrying that cursor into its first connection as `Last-Event-ID`, which changes the page's reconnect path. Doing that under this phase's remaining scope would have been a change to the one surface that has no browser to verify it on (C-7). The window remains microseconds wide and only before the first event of a connection; every reconnect after that carries `Last-Event-ID`, which is what §12 specifies and what is tested. **What would change it:** the three-part change above, taken as one task with T16's manual render checklist re-run. The comment at the bottom of `sse.stream()` still names this entry. |
| **T16-2 — `static/` is not package data** | **APPLIED, and it was worse than the entry said.** `[tool.setuptools.package-data]` now ships `shepherd.web` → `static/*` **and** `shepherd.store` → `migrations/*.sql` — the migration SQL had the identical defect and would have made an installed wheel unable to create its own database. `tests/test_packaging.py::test_every_static_asset_ships_in_the_wheel` reads the manifest as TOML (a key in the wrong table is exactly what a substring grep cannot see) and checks **every** file in `static/` against the declared patterns, so a new asset type cannot be silently dropped. `web/__init__.py` gained `static_root()` so the path is resolved beside the module — the way that works from a source tree and from a wheel. |

### New entries opened by T18

**T18-1 · `shepherd status` cannot see a running daemon's fleet** · `cli/` reaches
capabilities through `invoke()`, and the registry is a process-global (ADR-7), so a
`shepherd` process that `controld` did not start has no fleet tools; `status` exits 1
and says exactly that (`test_status_fails_when_no_tool_is_registered`). This phase
closed the two facts that need no store (T17-3 above) and deliberately did **not**
close the rest: projecting the fleet into a second process means either opening the
store there — which is a second writer thread against the database `controld` owns, and
§7 migration rule 3 exists to stop exactly that — or giving `cli/` an HTTP client for
the loopback API, which is a transport decision (D38's exporter, M4) and not a wiring
one · blocks: `shepherd status` being useful on a host where the daemon is running; the
fleet page is unaffected and is the shipped surface · owner: **M4 — decision gap**
  - **options:** (a) `cli/` reads the loopback API through a small client and renders
    `ToolResult`s the same way it does today — no new capability, one new transport;
    (b) an exporter at the `invoke()` seam (D38's own plan) that `cli/` consumes like
    any other consumer; (c) a read-only `Store` mode with no writer thread, so a
    consumer process can project without ever being able to write.
  - **recommendation (not applied):** (b), because D38 already says the exporter goes
    behind `invoke()` and changes no consumer file — building (a) first would be a
    transport `cli/` then has to unlearn. (c) is tempting and is the trap: "read-only"
    is a property of the code path, not of the file, and the first bug that writes
    through it is the bug D37 exists to make impossible.

**T18-2 · `PostToolBatch` is still not in `SUBSCRIBED_EVENTS`** · T11-2's refusal
counters (`TOOL_BLOCKED_BY_HOOK`, `TOOL_PERMISSION_REFUSED`) are built, tested and
correct, and they can never fire because nothing dispatches the event that carries the
evidence · plan gap M7 already says it should be subscribed · this phase did not add it:
subscribing a new hook event changes what gets written into the user's `settings.json`,
which is principle 4's surface and needs the installer's dry-run diff re-reviewed, not a
constant edited at the end of a build · blocks: `doctor` reporting how often a permission
refusal or hook block happened — the one thing `PostToolBatch` uniquely records · owner:
**T9/T18 → M2.** **What would change it:** adding `PostToolBatch` to `SUBSCRIBED_EVENTS`
together with a re-run of the installer's dry-run against a throwaway settings file, and
one live capture confirming the dispatcher's added per-event cost stays inside E34's
measured budget (principle 4: nothing we install may harm Claude Code).

**T18-3 · `controld.py` is exactly at ADR-1's 150-line cap** · **CLOSED (2026-09-17) by
the entry's own second option.** Tool registration moved to
`toolsurface/compose.py::compose_tool_surface(store, host, db_path, ingest)`, which
registers M1's capabilities and calls `freeze_registry()`; `controld` keeps the *order*,
which is the part that is a contract (compose → freeze → bind, ADR-7). The cap was not
raised: ADR-1's number is a proxy for "no logic in a composition root", and moving the
list of capabilities to where capabilities live costs nothing. `ingest` is a parameter
rather than composed inside because the socket's name belongs to `daemons/sessiond.py`
and L4 does not import L6 — the one deviation from the signature this entry proposed.
`controld.py` is now 148 lines, and the blank line PEP 8 wanted before
`if __name__ == "__main__":` is back. The 13 boundary rules are green with no exemption
added.


### New entries opened by the 2026-09-17 remediation (independent verification failed M1 on three defects)

**T18-4 · `is_live()` was always False, so §16's `running` bucket was unreachable** ·
**CLOSED.** `signals/ordering.py` declared `_STAMP = "%Y-%m-%dT%H:%M:%SZ"` and **no
production writer emitted that shape**: the discovery lane wrote `…30.138Z`, the hook
lane wrote `…48.078878+00:00`, and only the tests wrote the parser's own format. Every
`parse_stamp` swallowed its `ValueError` into `None`, `age_seconds` returned `None`,
`is_live` was `False` for every row always, and `effective_state` demoted every stored
`running` session to `starting` at an age of a few milliseconds. The suite stayed green
because every fixture hand-wrote the parser's format — the tests proved the parser
agreed with the tests · **fix:** one format, one writer, one reader.
`core/clock.py::utc_now()`/`stamp(moment)` emit `2026-09-17T01:04:18.671Z` (UTC,
millisecond precision, `Z`) and every writer now calls it — the hook lane's frame clock
(`ingest_socket.now_iso`), the discovery lane's scan clock and its epoch-ms conversion,
`tools_m1.utc_now`, `store/db`, `store/migrate`, and both host drivers'
`Liveness.observed_at`. `core/clock.parse_stamp` is the one reader; `ordering` and
`discovery_loop._seconds` both use it. The parser reads ISO-8601 rather than only the
canonical spelling, which is a *reader* tolerance for rows an older build wrote — not a
second writer · **evidence:** `tests/signals/test_liveness_stamp_contract.py`, whose
stamps come from the **real writers** and never from a literal: each production clock is
called and matched against the format as a literal regex; the hook lane is driven end to
end with `ingest_socket.now_iso` and the row comes back `running` through `fleet_tree`,
the projection the page consumes; the discovery lane is driven with the real
`07-sidecar-running.json` capture re-stamped to now; and `LinuxHost.process_liveness`
is called for real. Also proven against two real `controld` processes:
`"running" in projected states: True` (it was `False`) · **principle 5 note:**
`parse_stamp` still returns `None` rather than raising, and that is deliberate — but the
`None` is now documented at both seams as *unreadable*, distinct from *never stamped*,
and `is_live`'s `False` is only safe because the contract test above makes "unreadable"
mean "a row this build did not write". **Still open, deliberately:** an unreadable stamp
is not *counted* anywhere. `ordering` and `registry_delta` are pure and hold no store, so
counting one means plumbing an anomaly through the fold's result — worth doing, and not
worth doing at the end of a remediation · owner: **M2.**

**T18-5 · `python -m shepherd.cli.main` printed nothing and exited 0** · **CLOSED.** The
module had no `if __name__ == "__main__":` guard, so the canonical silent success:
`python -m shepherd.cli.main doctor` imported the module, ran no command, printed nothing
and exited `0`. `daemons/controld.py` and `daemons/sessiond.py` both had the guard; `cli/`
did not · the guard is added, and the regression is
`tests/test_packaging.py::test_every_entry_point_module_runs_as_a_module`, which runs
**every** `[project.scripts]` module as `python -m` with an argument it does not accept
and requires both output and a non-zero exit — so the next entry point added cannot
repeat it.

**T18-6 · the daemon could not restart on its own port once the page had connected** ·
**CLOSED.** `web/server.py` set `allow_reuse_address = False`, undocumented and untested.
The fleet page holds a permanent SSE connection and the server closes it on the way out,
so a socket in `TIME_WAIT` bearing the daemon's port as its *local* address is the
**normal** end state of any watched run: clean SIGTERM → exit 0 → immediate re-boot on
the same port → `OSError [Errno 98]` as a raw traceback, for up to two minutes. That is
D37's other half ("restart the control plane, keep the agents") broken in the common
case. The e2e suite missed it because every other test binds `port=0`, and an ephemeral
port is never the port that was just in use · **fix:** `allow_reuse_address = True`, with
the reason stated in the file. It does not widen §13 — `create_server` still refuses
every address but loopback, and `SO_REUSEADDR` is not `SO_REUSEPORT`: it permits
re-binding an address whose only remaining claimant is a closed connection of our own,
never two live listeners · **evidence:**
`tests/web/test_restart.py::test_the_daemon_restarts_on_its_own_port_after_a_page_connected`
(one fixed port, a real client connection, `TIME_WAIT` asserted present via
`/proc/net/tcp` before the re-bind so the test cannot pass against an empty table), and
`tests/daemons/test_controld.py::test_the_daemon_restarts_on_the_same_port_after_a_page_connected`
(the whole daemon, with an SSE subscriber attached). Both fail with `EADDRINUSE` when the
flag is flipped back. Proven out of process too: two real `controld` runs on one port,
first exit 0, `TIME-WAIT` present, second boot served the page and the session row
survived the restart.

**T18-7 · a refused start left a database and an unclosed store behind** · **CLOSED.**
`controld.start` opened the store *before* validating the socket plan, so a start refused
with `SocketPathTooLong` had already migrated a `shepherd.db` into place and started its
writer thread — a refusal that changed the disk is not a refusal. The order is reversed:
plan both sockets, then open the store. A port that is genuinely held is now refused the
way an over-long socket path already was — one line and exit `2`, not a traceback — and
the store is closed on that path · evidence:
`tests/daemons/test_controld.py::test_a_refused_start_leaves_no_database_behind` and
`::test_an_occupied_port_is_refused_honestly_not_as_a_traceback`.

**T18-8 · `pytest tests/web tests/toolsurface` failed at collection** · **CLOSED, and it
was wider than reported.** Two sibling test directories cannot both own the top-level
module name `conftest` under pytest's prepend import mode: whichever is imported first
wins, and `from conftest import Client` then reaches into the wrong directory's fixtures.
This is the same defect T12-1 fixed for `tests/golden/` by making the directory a
package. `tests/web/` and `tests/toolsurface/` are now packages — and so are
`tests/cli/`, `tests/signals/` and `tests/e2e/`, because `pytest tests/cli tests/signals`
failed identically and was not in the report. Every `from conftest import …` is now
`from <dir>.conftest import …`. Verified across four directory combinations plus the full
run and the live lane.

### Appended 2026-09-17 by M2 Task 18 (append only — nothing above was edited)

**T18-2 · RE-POINTED, not closed: `PostToolBatch` stays out of `SUBSCRIBED_EVENTS`**
· owner moves from **T9/T18 → M2** to **a milestone that can install hooks (M3 spawns
sessions; M6 owns packaging)**. M2 cannot close it: the checklist this entry wrote needs
the installer's dry-run diff re-reviewed against a **real** install on a **real** session,
and K3 forbids touching the user's `~/.claude/settings.json` — the rule T18-2 exists
because of. `SUBSCRIBED_EVENTS` is unchanged at **24** names, asserted now rather than
assumed: `tests/cli/test_doctor_idle_counters.py::test_post_tool_batch_is_still_not_subscribed`
fails the build if a later change subscribes it without reading this entry. **The
checklist is preserved verbatim:** add the name to `SUBSCRIBED_EVENTS`; re-review the
installer's dry-run diff against a throwaway settings file; re-run
`test_hookd_latency_under_10ms` against E34's 3.2 ms budget; assert the real settings
file's sha256 is unchanged. **What M2 paid instead** — the debt this entry left in the
product, not just in a ledger: `doctor` now renders `TOOL_BLOCKED_BY_HOOK` and
`TOOL_PERMISSION_REFUSED` as **`idle`**, never as `0`, with the reason under the line —
a zero that cannot move is a claim of coverage, and these two have never looked. The
event's *name* is absent from that line because §5.0's engine-vocabulary rule fails the
build on a hook event name in any `src/` literal outside `engines/`; see **T18-2a** in
`docs/plans/2026-09-17-m2-BLOCKERS.md`, which names the tool to add
(`hook_subscription_status`) so the line can one day say it.

**T18-3 · stays CLOSED, and the headroom is now a measured contract.** This entry was
closed at **148** lines — two under the cap, which satisfies the number and not the
constraint: the next wiring change could not be additive. M2 Task 18 took it to **136**,
with a **named 10-line reserve** for the stop path's construction and M2's tools, asserted
by `tests/daemons/test_controld_composition.py::test_the_composition_root_keeps_headroom_under_adr1s_cap`.
`toolsurface/compose.py` now composes both of ADR-7's module-globals (the registry *and*
the ring), `publish_result` moved there with them, and the root's last piece of real logic
— a poll loop waiting for the control socket to appear — is gone: `serve_control_ingest`
sets a defaulted `bound` event the moment it is listening. All 14 boundary rules are green
with no exemption added.

### Appended 2026-09-17 by M3 Task 22 (append only — nothing above was edited)

**T18-2 · CLOSED. The checklist this entry preserved was executed verbatim, against a
throwaway settings file. It was NOT re-pointed to M6.**

M2 re-pointed this entry to "a milestone that can install hooks" and preserved the
four-item checklist word for word. All four items ran. The "or" branch — a recorded
re-point to M6 with the failure named — was available and was **not** taken, because
nothing failed.

1. **The name is in `SUBSCRIBED_EVENTS`**, which is now **25** names. M1's 24 are
   unchanged and the count is written as `24 + 1` in the test so the arithmetic is
   visible rather than a number to trust.
   `tests/cli/test_doctor_idle_counters.py::test_post_tool_batch_is_still_not_subscribed`
   is **replaced** by
   `tests/engines/test_post_tool_batch_subscription.py::test_post_tool_batch_is_subscribed`,
   and the replacement names this task, so removing the name again fails the build in
   front of whoever does it.
2. **The installer's dry-run diff was re-reviewed against a POPULATED throwaway file**,
   not the shipped `{}`. `tests/e2e/conftest.py`'s `throwaway` fixture writes
   `settings.write_text("{}\n")` — its own docstring says "an explicit, empty settings
   file" — and a merge reviewed against `{}` cannot exhibit the risk this item exists to
   check: no pre-existing `hooks` key, no user entry to preserve, and none of
   `hooks_config.py`'s three stated properties exercised. **T24's `installed_settings`
   fixture does not exist in the tree yet**, so Task 22 built it: a throwaway copy
   carrying Shepherd's own installed block **produced by the shipped
   `install_hooks(settings_path, entry)` at the pre-change 24-name subscription** — never
   hand-written, because a hand-written block only proves the fixture agrees with the
   fixture — plus one foreign entry on `PreToolUse`, which is both a `CRITICAL_EVENT` and
   an event our own block also writes to, so it cannot survive by being somewhere we
   never touch. When T24 lands, that is the fixture to move rather than duplicate.

   The reviewed diff, taken 24 → 25 subscribed, is **purely additive**: one new
   `PostToolBatch` key carrying one `_shepherd_managed` matcher group with the host's own
   dispatch command and an explicit `timeout: 5` (E18/C7). **Nothing is removed**, the
   foreign `PreToolUse` entry is byte-identical afterwards, and the sibling `model` key
   survives. `install_hooks(..., dry_run=True)` wrote **zero bytes**, asserted by
   comparing the file's text before and after the dry run rather than by trusting the
   flag.
3. **`test_hookd_latency_under_10ms` was re-run at 25 subscribed events** and passes
   (exit 0). Measured means on this host, 25 invocations per attempt:
   **2.78, 3.05, 3.61 ms** — 3 of 3 inside the test's 10 ms budget and 2 of 3 inside
   E34's *measured* 3.2 ms, which is the test's own "2 of 3 attempts" rule. This is the
   expected result rather than a lucky one: the subscription count changes how many
   events the engine dispatches, not what one dispatch costs, and `hookd` never reads the
   list.
4. **The real `~/.claude/settings.json` is byte-identical**, sha256
   `375e53220773a12f0a2a7a666740f7fb20d31b2989b250e4076957d026b5d6ac`, unchanged before
   and after the whole suite, with no sibling appearing beside it.

**And the guard on item 4 was strengthened, because the old one could not have caught
T10-R1.** `tests/conftest.py` hashed the file before and after the session — an outcome
check, blind to a write whose bytes round-trip, which is exactly the shape of the planted
`path.write_text(path.read_text())` that ran against the user's live config on every
default test run. Task 22 adds a **`sys.addaudithook` over the whole suite** that records
every `open` of that exact path, both spellings of intent (`io.open`'s mode string and
`os.open`'s flags), and asserts at session end that **no write mode ever appears**. Its
negative control is structural and cannot rot: the digest guard itself opens the file for
reading, so an empty recording means the hook is dead, and the same assertion fails on
that. Proved live in the shadow tree with `CLAUDE_CONFIG_DIR` pointed at a throwaway
directory and T10-R1's exact round-trip write planted: the guard fires —
`something opened /tmp/fake-claude/settings.json for writing: [('w', 524865)]`, exit 1 —
against a file that is not the user's. The user's real file was never opened for writing
by anything in this task.

**The debt M2 paid in the product is now retired.** `doctor` rendered
`TOOL_BLOCKED_BY_HOOK` and `TOOL_PERMISSION_REFUSED` as `idle` with the reason, because
they were **structurally unable to move**. They can move now, and
`test_the_two_refusal_counters_can_now_move` proves it by driving a **real captured**
`PostToolBatch` payload — one per neutral `refusal` value, `permission` from
`S09_perm_default_write_outside` and `hook_block` from `R01_exit_codes`, both out of the
429 — through `HookLane.apply` and watching exactly one counter increment. That test
carries the load-bearing line `assert captured.event in SUBSCRIBED_EVENTS`: the fold has
always known how to count this frame, and without that assertion the test passes against
M1's build and proves nothing. So `IDLE_ANOMALIES`, `IDLE` and `IDLE_REASON` are **gone
from `cli/commands.py`**, not merely unused — left in place they are a loaded gun for the
next counter that looks dead, and the reason string still cited a closed blocker.
`test_the_idle_rendering_is_gone_not_merely_unused` fails if either name returns.

**Mutations run, each named and each red** (shadow tree `scratchpad/t21-t22/`): the 25th
name removed from `SUBSCRIBED_EVENTS` only (leaving `ALL_HOOK_EVENT_NAMES` intact — the
first attempt at this mutation edited the wrong tuple and was re-run, which is why the
two lists are asserted separately); the populated fixture replaced by `{}`, which turns
both the diff review and the foreign-entry survival red, i.e. the check really does
refuse a diff against nothing; and the audit hook never installed, which turns its own
liveness control red rather than silently reporting zero writes.

**Checkpoint.** Task 22's plan entry sets `human_verify` — "a human reads the dry-run
diff before it is applied anywhere". Nothing was applied anywhere: every install in this
task ran against a file under `tmp_path`, and no code path in the build writes to a real
settings file. The diff is reproduced in Task 22's return for that reading.
